import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card';

export function Dispositions({ patientId }) {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Dispositions</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Dispositions section</p>
      </CardContent>
    </Card>
  );
}


